import React from 'react'

const Pagination = ({shoesPerPage,totalShoes}) => {
    const pageNumbers = []

    
    for(let i =1; i <= Math.ceil(totalShoes/shoesPerPage); i++) {

    }

  return (
    <div>Pagination</div>
  )
}

export default Pagination